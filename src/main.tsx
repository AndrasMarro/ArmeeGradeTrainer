import React from 'react';
import ReactDOM from 'react-dom/client';
import SwissArmyRanksApp from './swiss-army-ranks.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SwissArmyRanksApp />
  </React.StrictMode>,
);
